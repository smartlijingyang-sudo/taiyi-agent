[project]
name = "taiyi-cosmokit"
version = "1.0.0"
description = "通用工具：priority queue, Random, make_array, observe（vendored cosmokit 的 Python 等价）"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cosmokit"]