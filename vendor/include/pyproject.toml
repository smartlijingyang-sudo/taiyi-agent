[project]
name = "taiyi-include"
version = "0.0.0"
description = "Placeholder; populated in a later chunk"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/include"]
