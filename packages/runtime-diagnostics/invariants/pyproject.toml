[project]
name = "taiyi-runtime-diagnostics-invariants"
version = "0.1.0"
description = "taiyi-runtime-diagnostics-invariants — invariant registry and test hook"
requires-python = ">=3.11"
dependencies = [
    "taiyi-cordis",
    "taiyi-cosmokit",
    "taiyi-schemastery",
    "taiyi-loader",
    "taiyi-include",
    "taiyi-group",
    "taiyi-timer",
    "taiyi-hmr",
    "taiyi-logger-console",
    "taiyi-core-scope",
    "taiyi-core-session",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/taiyi_runtime_diagnostics_invariants"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
filterwarnings = [
    "ignore:coroutine 'Fiber._reload' was never awaited:RuntimeWarning",
]

[tool.coverage.run]
source = ["src/taiyi_runtime_diagnostics_invariants"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if __name__ == .__main__.",
    "if TYPE_CHECKING:",
]

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "N", "ASYNC"]
ignore = ["E501", "B008"]

[tool.pyright]
pythonVersion = "3.11"
strict = ["src/taiyi_runtime_diagnostics_invariants", "tests"]
exclude = ["invariant"]