[project]
name = "taiyi-core-scope"
version = "0.1.0"
description = "per-agent 隔离注册原语"
requires-python = ">=3.11"
dependencies = ["taiyi-cordis"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/taiyi_core_scope"]