[project]
name = "taiyi-llm-retry"
version = "0.1.0"
description = "指数退避重试 provider 装饰器"
requires-python = ">=3.11"
dependencies = ["taiyi-llm"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/taiyi_llm_retry"]