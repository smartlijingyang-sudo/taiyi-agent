[project]
name = "taiyi-cli"
version = "0.1.0"
description = "taiyi CLI entry point"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/taiyi_cli"]